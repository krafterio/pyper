<?xml version="1.0"?>
<odoo>
    <record id="rule_mail_message_access_restriction_partner" model="ir.rule">
        <field name="name">Mail message access restriction rule</field>
        <field name="model_id" ref="mail.model_mail_message"/>
        <field name="domain_force">['|', ('restrict_access', '=', False), ('partner_ids', 'in', user.partner_id.id)]</field>
        <field name="groups" eval="[Command.link(ref('base.group_user'))]"/>
    </record>
</odoo>
