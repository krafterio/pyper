<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="1024"/>
        <field name="inherit_id" ref="base_setup.res_config_settings_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//app[@name='pyper_settings']" position="inside">
                <block title="Overlay Menu" name="pyper_overlay_menu_settings" groups="base.group_erp_manager">
                    <setting help="Display only the root menu items in Overlay Menu as applications with icons">
                        <field name="overlay_menu_show_root_app"/>
                    </setting>

                    <setting help="Display the web icon of menu items">
                        <field name="overlay_menu_prefer_web_icon"/>
                    </setting>

                    <setting help="Hide the menu items if the category is not defined or is in global category">
                        <field name="overlay_menu_hide_empty_category"/>
                    </setting>
                </block>
            </xpath>
        </field>
    </record>
</odoo>
