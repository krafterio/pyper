<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record model="ir.ui.view" id="view_move_line_form">
        <field name="name">account.move.line.form.inherit</field>
        <field name="model">account.move.line</field>
        <field name="inherit_id" ref="account.view_move_line_form" />
        <field name="priority" eval="20" />
        <field name="arch" type="xml">
            <field name="date" position="after">
                <field name="date_range_fy_id" optional="hide" options="{'no_open': True}" />
            </field>
        </field>
    </record>

    <record model="ir.ui.view" id="view_account_move_line_filter">
        <field name="name">account.move.line.search.inherit</field>
        <field name="model">account.move.line</field>
        <field name="inherit_id" ref="account.view_account_move_line_filter" />
        <field name="priority" eval="20" />
        <field name="arch" type="xml">
            <field name="date" position="after">
                <field name="date_range_fy_id"/>
            </field>
        </field>
    </record>

    <record model="ir.ui.view" id="view_move_line_tree">
        <field name="name">account.move.line.tree.inherit</field>
        <field name="model">account.move.line</field>
        <field name="inherit_id" ref="account.view_move_line_tree" />
        <field name="priority" eval="20" />
        <field name="arch" type="xml">
            <field name="date" position="after">
                <field name="date_range_fy_id" optional="hide"/>
            </field>
        </field>
    </record>
</odoo>
