<?xml version="1.0"?>
<odoo>
    <record id="organization_rule" model="ir.rule">
        <field name="name">multi-company organization rule</field>
        <field name="model_id" ref="model_organization"/>
        <field name="domain_force">['|',('company_id','=',False),'&amp;',('company_id', 'in', company_ids),('user_ids', 'in', user.id)]</field>
    </record>
</odoo>
