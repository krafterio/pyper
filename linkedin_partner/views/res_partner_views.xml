<odoo>
    <record id="linkedin_partner.view_partner_form" model="ir.ui.view">
        <field name="name">res.partner.custom.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//group[@name='contact_form_group']/field[@name='function']" position="after">
                <field name="linkedin_url" placeholder="LinkedIn profil URL" invisible="type != 'contact'"/>
            </xpath>

            <xpath expr="//field[@name='function']" position="after">
                <field name="company_linkedin_url" string="Linkedin URL"
                       placeholder="Linkedin URL" widget="url"
                       invisible="is_company == False"/>
                <field name="linkedin_url" string="Linkedin URL" widget="url"
                       invisible="is_company == True"/>
            </xpath>
        </field>
    </record>
</odoo>